[tool.poetry]
name = "requestmanager-python"
version = "0.1.0"
description = ""
authors = ["Jesper Reiche <jesper@jesperreiche.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
